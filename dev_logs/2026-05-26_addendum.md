# 2026-05-26 补充开发记录

## 本轮完成

- 确认后端已真实连接 PostgreSQL + `pgvector`
- 完成 grounded QA 引用链路与编辑能力验收
- 完成 Phase 4 最小可用版内容摄取：
  - 新增 `POST /api/v1/ingest/url`
  - 新增 `POST /api/v1/ingest/pdf`
  - 支持网页正文抓取、PDF 文本提取、入库、切块与检索链路复用
  - 记录详情页可展示导入来源链接或 PDF 文件名
  - 收集页新增“导入网页 / 导入 PDF”入口
- 完成 Phase 5 第一小步“主动提醒 MVP”：
  - 新增 `GET /api/v1/dashboard/reminders`
  - 首页新增系统提醒卡片区，优先提示逾期、24 小时内到期、高优先级和阻塞待办
  - 待办编辑支持设置提醒时间 / 截止时间
  - 新建待办时可直接设置时间
- 重写并修正部分关键前端页面，减少历史乱码影响：
  - `InboxPage`
  - `DashboardPage`
  - `MemoryDetailPage`
  - `QuickCaptureCard`
  - `TodosPage`

## 本轮验证

- `frontend`: `npm run build` 成功
- `backend`: `python -c "from app.services.dashboard_service import dashboard_service; from app.api.v1.dashboard import router; print('dashboard_ok')"` 成功
- 真实 HTTP 验证 Phase 4：
  - 注册新用户
  - 成功导入 `https://example.com`
  - 成功上传测试 PDF
  - `/memories` 可检索导入结果
  - `/qa/ask` 可引用网页与 PDF 记录
- 真实 HTTP 验证 Phase 5：
  - 创建逾期待办、24 小时内到期待办、高优先级待办
  - `/dashboard/reminders` 成功返回提醒结果
  - `/dashboard/overview` 统计同步更新

## 当前阶段判断

- Phase 0：完成
- Phase 1：完成
- Phase 2：完成
- Phase 3：完成（MVP 范围内）
- Phase 4：完成（最小可用版）
- Phase 5：完成第一小步 MVP

## 当前遗留

- `hybrid_web` 仍未接入真实联网检索
- Phase 5 剩余项：
  - 外部通知通道（如微信 / PushDeer）
  - review queue 后台触发机制再增强
- Phase 6 尚未开始完整收尾：
  - 演示闭环打磨
  - 文案与乱码清理
  - 更系统的验收与部署说明

## 下一步建议

- 优先补外部通知通道，形成真正的“主动提醒”
- 之后集中清理剩余乱码文案，统一中文体验

## 2026-05-26 本轮追加进展（Phase 5 后台任务）

### 已完成

- 新增持久化提醒事件表：`reminder_events`
- 新增后台提醒服务：
  - `backend/app/services/reminder_service.py`
  - `backend/app/services/reminder_scheduler.py`
- 服务启动后会自动开启后台扫描循环
- 扫描结果会写入数据库，而不是仅在首页临时计算
- 首页 `/dashboard/reminders` 已改为优先读取持久化提醒结果
- 启动初始化时会同步一次提醒，避免首次打开首页时为空

### 本轮验证

- `backend`: `python -c "from app.services.reminder_scheduler import reminder_scheduler; from app.services.reminder_service import reminder_service; from app.models.reminder import ReminderEvent; print('reminder_stack_ok')"` 成功
- 重启后端到最新代码成功，`/health` 返回正常
- 真实 HTTP 验证：
  - 注册新用户
  - 创建逾期待办和 24 小时内到期待办
  - `/dashboard/reminders` 返回对应提醒
  - `/dashboard/overview` 统计同步更新
- 数据库验证：
  - `reminder_events` 表中已写入新提醒事件记录

### 阶段判断更新

- Phase 5：
  - 实时提醒计算：完成
  - 后台定时扫描：完成
  - 外部通知通道：未完成

## 2026-05-26 本轮追加进展（Phase 5 外部通知通道）

### 已完成

- 新增 PushDeer 外部提醒通知通道：
  - `backend/app/services/notification_service.py`
  - 支持扫描 `sent_at is null` 的 active reminder event 并投递
  - 投递成功后写入 `sent_at`，避免重复发送
- 后台提醒调度器已接入通知派发：
  - `backend/app/services/reminder_scheduler.py`
  - 生成提醒后会继续尝试发送未投递提醒
- 新增配置项：
  - `REMINDER_NOTIFICATION_ENABLED`
  - `REMINDER_NOTIFICATION_TIMEOUT_SECONDS`
  - `PUSHDEER_ENDPOINT`
  - `PUSHDEER_PUSHKEY`
- 更新 `backend/.env.example` 与 `README.md`，补充 PushDeer 启用方式。

### 本轮验证

- `D:\py\anaconda\python.exe -m py_compile ...` 通过
- 新增通知服务与 scheduler 导入检查通过：
  - `notification_stack_ok`

### 阶段判断更新

- Phase 5：
  - 实时提醒计算：完成
  - 后台定时扫描：完成
  - 外部通知通道：完成最小可用版
  - 后续可增强：用户级通知偏好、失败重试、发送历史页面、更多通道

## 2026-05-26 本轮追加进展（用户级通知偏好）

### 已完成

- 新增设置更新接口：
  - `PATCH /api/v1/settings`
  - 当前支持保存 `notify_channels`
- 通知派发接入用户偏好：
  - 仅当用户设置中包含 `pushdeer` 时，后台任务才会投递 PushDeer 通知
  - 新用户默认只开启 `in_app`，避免全局 PushDeer key 对所有用户默认外推
- 前端设置页新增提醒方式编辑：
  - `in_app` 默认保留
  - `pushdeer` 可开关
  - 保存后刷新 settings cache

### 本轮验证

- 后端编译检查通过
- 后端 settings router / notification service 导入检查通过：
  - `settings_notification_ok`
- 前端 `npm.cmd run build` 通过

### 阶段判断更新

- Phase 5：
  - 实时提醒计算：完成
  - 后台定时扫描：完成
  - 外部通知通道：完成最小可用版
  - 用户级通知偏好：完成最小可用版

## 2026-05-26 本轮追加进展（Review Queue 后台触发）

### 已完成

- Review Queue 触发逻辑从 `MemoryService` 内联创建，收拢到 `ReviewService`：
  - `enqueue_for_memory`
  - `sync_all_users`
- 新增 Review Queue 去重能力：
  - 同一用户、同一目标、同一 review type 只保留一个 pending 项
- 后台调度器接入 Review Queue 补扫：
  - 服务启动初始化会补扫 active memories
  - 后台扫描循环会持续补齐缺失待确认项
- 当前 MVP 规则：
  - 低置信度记忆理解进入 `low_confidence`
  - 项目类记录进入 `relation_confirmation`
  - 疑似待办记录进入 `todo_approval`
- 前端补充 `pushdeer` 通知渠道中文展示。
- README 已同步 Review Queue 后台触发说明。

### 本轮验证

- 后端编译检查通过
- 后端 Review Queue / MemoryService / Scheduler 导入检查通过：
  - `review_background_ok`
- 前端 `npm.cmd run build` 通过

### 阶段判断更新

- Phase 5：
  - 实时提醒计算：完成
  - 后台定时扫描：完成
  - 外部通知通道：完成最小可用版
  - 用户级通知偏好：完成最小可用版
  - Review Queue 后台触发：完成最小可用版

## 2026-05-26 本轮追加进展（Review Queue 操作闭环）

### 已完成

- 新增 Review Queue 用户处理动作：
  - `POST /api/v1/review-queue/{review_id}/confirm`
  - `POST /api/v1/review-queue/{review_id}/ignore`
  - `POST /api/v1/review-queue/{review_id}/convert-to-todo`
- 后端支持：
  - 读取单条 review item
  - 写入 `user_decision`
  - 更新状态为 `confirmed` / `rejected`
  - 从待确认项生成 TODO，并保留来源 memory / review metadata
  - 写入 timeline 记录
- 前端 Review 页面新增操作按钮：
  - 确认
  - 转成 TODO
  - 忽略
- 操作后自动刷新：
  - reviews
  - todos
  - timeline
  - dashboard overview

### 本轮验证

- 后端编译检查通过
- 后端 Review Queue action router / service 导入检查通过：
  - `review_actions_ok`
- 前端 `npm.cmd run build` 通过

### 阶段判断更新

- Phase 5：
  - Review Queue 生成：完成
  - Review Queue 用户处理：完成最小可用版
  - 微信/外部通知接入：可以开始进入专门通道实现

## 2026-05-26 本轮追加进展（通知 Provider 与企业微信 Webhook）

### 已完成

- 重构提醒外部通知为 provider 架构：
  - `NotificationProvider`
  - `PushDeerProvider`
  - `WeComWebhookProvider`
- 新增企业微信机器人 Webhook 通道：
  - 配置项：`WECOM_WEBHOOK_URL`
  - 用户通知偏好通道：`wecom`
  - 前端设置页可开启 / 关闭企业微信通道
- PushDeer 与企业微信可并存：
  - 后台扫描未发送提醒
  - 按用户 `notify_channels` 判断允许通道
  - 至少一个外部通道投递成功后写入 `sent_at`
- 更新 README 和 `.env.example`。

### 本轮验证

- 后端编译检查通过
- 后端通知 provider 导入检查通过：
  - `notification_providers_ok`
- 前端 `npm.cmd run build` 通过

### 阶段判断更新

- 微信相关接入：
  - PushDeer：完成最小可用版
  - 企业微信机器人 Webhook：完成最小可用版
  - 公众号 / 测试号模板消息：尚未开始，需要 app_id、app_secret、模板 ID、用户 openid 与公网回调策略

## 2026-05-26 本轮追加进展（PushDeer 端到端验证）

### 已完成

- 确认 `backend/.env` 中 `PUSHDEER_PUSHKEY` 与 `REMINDER_NOTIFICATION_ENABLED` 已配置。
- 确认后端在 `backend` 运行目录下可以正确读取 PushDeer 配置。
- 使用 `PushDeerProvider` 发送连通性测试成功。
- 创建真实逾期 TODO 并触发：
  - reminder 生成
  - PushDeer 投递
  - `sent_at` 写入
- 修复提醒去重逻辑 bug：
  - `reminder_service` 之前比较 `reminder_payload["title"]`，但 payload 未提供 `title`
  - 已统一补齐 payload title，并整理提醒消息生成逻辑

### 本轮验证

- PushDeer 连通性测试：
  - `pushdeer_send_ok=True`
- Demo 用户通知渠道：
  - `in_app,pushdeer`
- 真实提醒链路：
  - `reminders_created=4`
  - `notifications_sent=9`
  - 测试 TODO 对应 reminder 已写入 `sent_at`
- 后端编译与导入检查通过：
  - `reminder_notification_import_ok`

### 注意事项

- 本次真实链路验证会补发历史未发送 reminder，因此手机端可能收到多条 PushDeer。
- 企业微信通道代码已完成，但当前 `.env` 尚未配置 `WECOM_WEBHOOK_URL`。

## 2026-05-26 本轮追加进展（Server酱 Turbo 微信通知）

### 已完成

- 新增 Server酱 Turbo 通知 provider：
  - `ServerChanProvider`
  - 通知渠道名：`serverchan`
  - 配置项：`SERVERCHAN_SENDKEY`
  - 默认接口：`https://sctapi.ftqq.com/{SENDKEY}.send`
- Server酱已接入统一通知 provider 架构：
  - 与 PushDeer / 企业微信 Webhook 可并存
  - 受用户 `notify_channels` 控制
  - 投递成功后沿用 reminder `sent_at` 防重复机制
- 前端设置页新增 Server酱微信渠道开关。
- README 与 `.env.example` 已补充 Server酱配置说明。

### 本轮验证

- 后端编译检查通过
- 后端 Server酱 provider 导入检查通过：
  - `serverchan_provider_ok`
- 前端 `npm.cmd run build` 通过

### 注意事项

- SendKey 属于敏感信息，不写入日志。
- 若 SendKey 已在对话中暴露，建议后续在 Server酱后台重新生成或轮换。

## 2026-05-26 本轮追加进展（Server酱连通性验证）

### 已完成

- 确认 `backend/.env` 中 `SERVERCHAN_SENDKEY` 与 `SERVERCHAN_ENDPOINT_BASE` 已配置。
- 使用 Node 网络栈完成 Server酱 Turbo 真实发送测试。
- Server酱返回 HTTP 200，业务响应 `code=0`，表示发送成功。

### 注意事项

- 本轮 PowerShell / curl 在本机 schannel 层失败，未拿到业务响应；Node 请求成功。
- 日志不记录 SendKey、readkey 等敏感返回字段。

## 2026-05-26 本轮追加进展（设置页通知测试闭环）

### 已完成

- 新增通知 provider 状态接口：
  - `GET /api/v1/settings/notification-providers`
  - 返回外部通知渠道是否已配置
- 新增单次测试通知接口：
  - `POST /api/v1/settings/notifications/test`
  - 请求体：`channel`
  - 仅允许测试当前用户已启用的外部渠道
- 设置页新增外部渠道测试按钮：
  - PushDeer
  - Server酱微信
  - 企业微信
- 测试按钮会根据 provider 配置状态与用户启用状态自动禁用。
- README 已补充“设置页可发送单次测试通知”的说明。

### 本轮验证

- 前端 `npm.cmd run build` 通过。
- 当前沙箱拒绝执行本机 Python，因此后端 `py_compile` / 导入检查本轮未能执行；需要在后端运行环境恢复后补跑。

## 2026-05-26 本轮追加进展（Phase 6 smoke test 脚本）

### 已完成

- 新增 Phase 6 演示闭环 smoke test：
  - `backend/scripts/smoke_test_phase6.py`
- 默认验证链路：
  - demo 登录
  - 读取设置
  - 读取通知 provider 配置状态
  - 创建项目类 memory
  - 检查 Review Queue 自动生成 pending item
  - 将 review item 转成 TODO
  - 检查 TODO 列表中存在转换结果
- 外部通知测试默认不发送，避免消耗额度。
- 可选参数支持单次真实通知测试：
  - `--send-notification serverchan`
  - `--send-notification pushdeer`
  - `--send-notification wecom`
- README 已补充 smoke test 命令。

### 本轮验证

- 文件创建确认通过。
- README 命令检查通过。
- 前端 `npm.cmd run build` 通过。
- 当前沙箱拒绝执行本机 Python，因此 `smoke_test_phase6.py` 的 `py_compile` 与真实执行本轮未能运行。

## 2026-05-26 本轮追加进展（Phase 6 smoke test 补验）

### 已完成

- 权限恢复后补跑后端编译检查。
- 补跑后端导入检查。
- 发现 `127.0.0.1:8000` 当前运行的是旧后端进程，未加载新增 settings notification provider 接口。
- 未打断旧服务，临时启动新版后端到 `127.0.0.1:8001` 完成 Phase 6 smoke test，结束后关闭临时进程。

### 本轮验证

- 后端 `py_compile` 通过。
- 后端导入检查通过：
  - `backend_import_ok`
- 前端 `npm.cmd run build` 通过。
- `python scripts/smoke_test_phase6.py --base-url http://127.0.0.1:8001/api/v1` 通过：
  - `auth=ok`
  - `configured_providers=['pushdeer', 'serverchan']`
  - `memory_create=ok`
  - `review_queue=ok:relation_confirmation`
  - `review_convert_to_todo=ok`
  - `todo_conversion=ok`
  - `status=ok`

### 注意事项

- 当前 `127.0.0.1:8000` 需要重启后才会加载最新接口。

## 2026-05-26 本轮追加进展（本地后端更新到最新代码）

### 已完成

- 停止旧的 `127.0.0.1:8000` 后端进程。
- 使用最新代码重新启动后端到 `127.0.0.1:8000`。
- 确认新增 settings notification provider 接口已加载；未登录访问返回认证要求，而不是旧服务的 404。
- 在最新 `8000` 服务上补跑 Phase 6 smoke test。

### 本轮验证

- `python scripts/smoke_test_phase6.py --base-url http://127.0.0.1:8000/api/v1` 通过：
  - `auth=ok`
  - `configured_providers=['pushdeer', 'serverchan']`
  - `memory_create=ok`
  - `review_queue=ok:relation_confirmation`
  - `review_convert_to_todo=ok`
  - `todo_conversion=ok`
  - `status=ok`

### 当前本地运行状态

- 后端：`http://127.0.0.1:8000`
- 当前进程已加载最新通知测试接口与 Phase 6 相关改动。

## 2026-05-26 本轮追加进展（设置页通知状态打磨）

### 已完成

- 设置页提醒方式从简单复选框改为渠道状态列表。
- 每个通知渠道现在展示：
  - 渠道名称
  - 是否已配置
  - 是否已开启
  - 单独测试按钮
- 测试通知反馈按渠道展示：
  - 发送成功显示对应渠道名
  - 发送失败提示检查渠道配置
- 移动端设置页渠道行改为单列布局，避免窄屏拥挤。

### 本轮验证

- 后端 `py_compile` 通过。
- 前端 `npm.cmd run build` 通过。
- Phase 6 smoke test 通过：
  - `auth=ok`
  - `configured_providers=['pushdeer', 'serverchan']`
  - `review_queue=ok:relation_confirmation`
  - `todo_conversion=ok`
  - `status=ok`
- 前端本地服务 `http://127.0.0.1:5173` 返回 200。

### 注意事项

- 本轮浏览器自动化工具不可用，Node 环境也未安装 Playwright，因此未做截图级验证。

## 2026-05-26 本轮追加进展（整体 UI/UX 视觉体系重构）

### 已完成

- 重构应用 Shell：
  - 固定侧栏
  - 顶部上下文标题区
  - 统一新增入口
  - 更清晰的页面信息层级
- 重写侧栏视觉：
  - 新品牌标识块
  - 更克制的导航状态
  - 用户信息区压缩为工具型布局
- 重构全局设计 token：
  - 低饱和灰阶背景
  - 克制主色点缀
  - 统一文字、边框、阴影变量
  - 移除旧的径向装饰背景
- 统一组件视觉：
  - Card / Panel / List Item
  - Button / Ghost Button
  - Input / Textarea / Select
  - Stat Card
  - Empty / Loading / Error
  - Reminder / Citation / Message
- 响应式优化：
  - Tablet 下侧栏转顶部导航区
  - Mobile 下内容单列化
  - 设置页通知渠道行移动端自动堆叠
- 视觉一致性清理：
  - 卡片半径统一为 8px 系统
  - 扫描并清理旧大圆角规则
  - 扫描并确认前端源码无明显乱码残留

### 本轮验证

- 前端 `npm.cmd run build` 通过。
- 后端 Phase 6 smoke test 通过：
  - `auth=ok`
  - `configured_providers=['pushdeer', 'serverchan']`
  - `review_queue=ok:relation_confirmation`
  - `todo_conversion=ok`
  - `status=ok`
- 前端本地服务 `http://127.0.0.1:5173` 返回 200。
- 静态扫描通过：
  - 无旧径向装饰背景残留
  - 无旧大圆角规则残留
  - 前端源码未检出明显乱码模式

### 注意事项

- 本轮主要保持业务逻辑与数据流不变，集中在视觉架构、布局、设计 token 与组件样式。
- 当前会话没有可用浏览器自动化工具，因此未做截图级视觉验收；后续可在真实浏览器中逐页检查并微调。

## 2026-05-26 本轮追加进展（深色 AI SaaS 视觉语言重构）

### 已完成

- 将整体视觉从浅色工作台切换为深色高级 AI 产品主题：
  - 深灰黑主背景
  - 蓝紫 / 青蓝低饱和点缀
  - 半透明暗色玻璃卡片
  - 细边框、深层级阴影与克制光效
- 重构 AppShell：
  - AI Agent Workspace 品牌壳层
  - 沉浸式固定侧栏
  - 顶部 Agent 状态与新增入口
  - 移动端导航抽屉开合
- 重构全局视觉系统：
  - `color-scheme: dark`
  - 暗色 token
  - Card / Panel / Button / Form / Input / List / Message / Citation / Reminder / Settings 统一暗色材质
  - Loading 增加轻量 skeleton sweep 动效
  - 页面进入增加短动画
- 视觉一致性清理：
  - 清理浅色背景残留
  - 扫描确认无旧纯白 / 近白背景规则残留
  - 扫描确认前端源码无明显乱码模式

### 本轮验证

- 前端 `npm.cmd run build` 通过。
- 后端 Phase 6 smoke test 通过：
  - `auth=ok`
  - `configured_providers=['pushdeer', 'serverchan']`
  - `review_queue=ok:relation_confirmation`
  - `todo_conversion=ok`
  - `status=ok`
- 前端本地服务 `http://127.0.0.1:5173` 返回 200。

### 注意事项

- 本轮没有改动核心业务数据流。
- 当前没有可用浏览器自动化截图工具，因此视觉结果建议在真实浏览器中再做逐页人工验收。

## 2026-05-26 本轮追加进展（Phase 7-16 长期路线图）

### 已完成

- 新增 AI Agent 长期演进路线图：
  - `docs/standards/11_AI_Agent长期演进路线图.md`
- 路线图覆盖接下来 10 个阶段：
  - Phase 7：Memory Intelligence Layer
  - Phase 8：Structured Facts & Temporal Index
  - Phase 9：Retrieval Evaluation Harness
  - Phase 10：Hybrid Retrieval & Memory Graph
  - Phase 11：Agentic RAG Workflow
  - Phase 12：Personal Insight Agents
  - Phase 13：Long-term Memory & Reflection
  - Phase 14：Proactive Workflow System
  - Phase 15：Multi-Agent Orchestration
  - Phase 16：Personal OS Productization
- 每个阶段均补充：
  - 核心目标
  - 必须实现
  - 技术重点
  - UI/UX 重点
  - 先不做
  - 退出条件
- 更新文档入口：
  - `docs/standards/00_文档导航.md`
  - `docs/standards/10_分阶段开发路线图.md`
  - `PROJECT_WORK_GUIDE.md`

### 本轮验证

- 确认新路线图文件已创建。
- 确认文档导航包含新路线图。
- 确认当前阶段路线图指向 Phase 7。
- 确认项目工作指南已切换到 Post-MVP AI Agent 演进顺序。

## 2026-05-27 本轮追加进展（Phase 7 Memory Intelligence Layer 起步）

### 已完成

- 新增结构化事实模型：
  - `ExtractedFact`
  - 表名：`extracted_facts`
  - 支持 `fact_type`、`structured_payload`、`confidence_score`、`event_time`、`source`
- 新增事实仓储：
  - `backend/app/repos/fact_repo.py`
- 新增规则型事实抽取服务 v1：
  - `backend/app/services/fact_extraction_service.py`
  - 支持最小四类事实：
    - `expense`
    - `mood`
    - `learning`
    - `plan`
- 接入 Memory 创建 / 更新链路：
  - 新建记忆后自动抽取 facts
  - 更新记忆后重建 facts
- API 返回增强：
  - `MemoryDetail.extracted_facts`
- 前端记忆详情页新增：
  - “AI 理解结果”
  - fact 类型、结构化摘要、置信度、事件时间、来源
- 新增 Phase 7 facts smoke test：
  - `backend/scripts/smoke_test_phase7_facts.py`
- README 已补充 Phase 7 smoke test 命令。

### 本轮验证

- 后端 `py_compile` 通过。
- 前端 `npm.cmd run build` 通过。
- 后端导入检查通过：
  - `phase7_import_ok`
- 重启本地后端到最新代码。
- Phase 6 回归 smoke test 通过：
  - `status=ok`
- Phase 7 facts smoke test 通过：
  - `expense_facts=['expense']`
  - `mood_facts=['mood', 'mood']`
  - `learning_facts=['learning']`
  - `plan_facts=['plan']`
  - `status=ok`
- 前端源码乱码扫描通过。
- 前端 dev server 已启动，`http://127.0.0.1:5173` 返回 200。

### 注意事项

- 当前事实抽取是 rule-based v1，用于建立数据结构与闭环；后续应升级为 LLM schema extraction + eval。
- `mood` case 可能抽取多条 mood fact，这是预期行为，例如睡眠不好与疲惫可同时存在。
