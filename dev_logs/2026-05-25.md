# 2026-05-25 开发日志

## 今日完成

- 完成 `MySecondBrain` 需求分析文档 V1.1，并补齐数据库、API、页面、Agent、RAG、部署、测试和版本规划内容。
- 创建前端 React + Vite 工作台骨架，补齐 Dashboard、Inbox、Memories、Memory Detail、Todos、Timeline、AI Chat、Review、Settings 页面路由。
- 创建后端 FastAPI 骨架，提供 `dashboard / memories / todos / timeline / qa / review-queue / settings` 基础接口。
- 建立后端 mock store，打通最小展示与交互闭环。
- 增加 SQLAlchemy + pgvector ORM 骨架，为后续接 PostgreSQL 做准备。
- 建立项目执行规范基础设施，包括：
  - `PROJECT_WORK_GUIDE.md`
  - `docs/standards/*`
  - `dev_logs/*`
- 创建每日自动开发日志任务 `MySecondBrain Daily Dev Log`，用于每天更新完成事项与待办事项。
- 完成 Phase 1 核心持久化替换：
  - 移除后端主链路对内存 `mock store` 的依赖
  - 新增 SQLAlchemy `session / repo / service / bootstrap`
  - 将 `dashboard / memories / todos / timeline / review-queue / settings / qa` 改为数据库驱动
  - 增加本地 SQLite 默认持久化与 PostgreSQL 配置切换能力
  - 增加 `backend/scripts/init_db.py` 和 `backend/migrations/README.md`
- 完成 Phase 2 后端认证与用户隔离基础能力：
  - 新增注册、登录、`/auth/me`
  - 新增密码哈希与 JWT
  - 新增 Bearer token 解析
  - 将 `user_id` 依赖切换为 token 优先、开发 demo fallback 次之
  - 验证新注册用户与 demo 用户的数据隔离
- 完成 Phase 3 第一小步：
  - 新增 `memory_chunks` repository 与 chunk service
  - 新建记忆自动生成 chunks
  - 启动时自动回填历史记忆缺失 chunks
  - QA 从 chunk 层执行第一版基础召回，并返回 chunk 摘要型 citations
- 完成第一轮前端体验改版，方向从“技术工作台”切换为“中文个人备忘助手”：
  - 重写全局视觉样式，改为浅色、轻量、低压迫感的界面
  - 统一首页、记录、待办、时间线、问答、待确认、设置页的中文表达
  - 首页加入“一句话快速记录”主入口，降低首次使用门槛
  - 待办、时间线、问答页改为更贴近日常用户的结构和文案
  - AI 问答页补充引用来源卡片与追问建议展示

## 验证记录

- 前端执行 `npm run build` 成功。
- 后端 `http://127.0.0.1:8000/health` 返回 200。
- 前端 `http://127.0.0.1:5173` 返回 200。
- 后端核心接口已验证可返回基础 JSON 数据：
  - `/api/v1/dashboard/overview`
  - `/api/v1/memories`
  - `/api/v1/qa/ask`
- ORM 模型导入成功，已修复 SQLAlchemy `metadata` 保留字冲突问题。
- `python scripts/init_db.py` 可成功初始化数据库。
- SQLAlchemy 持久化验证通过：创建记忆会写入数据库，并联动写入 `timeline_events / review_queue`。
- TODO 完成后可写入时间线记录。
- 认证链路验证通过：
  - 注册新用户成功
  - 登录 demo 用户成功
  - `/auth/me` 可返回当前用户
  - 新用户创建的数据不会出现在 demo 用户记忆列表中
- 在保留开发兜底模式的情况下，现有前端仍可正常构建并访问核心接口。
- chunk 持久化验证通过：
  - 历史数据启动后自动生成 `memory_chunks`
  - 新建记忆后可生成新的 chunk 记录
  - QA 可命中 chunk 层内容并返回相关 citations
- 前端 UI 改版后再次执行 `npm run build` 成功。
- 本地前后端服务持续可用：
  - `http://127.0.0.1:5173`
  - `http://127.0.0.1:8000`

## 风险 / 阻塞

- 当前本地默认数据库是 SQLite，尚未切换到真实 PostgreSQL 服务实例。
- QA 已切到 chunk 层基础召回，但仍未接入真实 embedding 与 LLM 最终生成。
- 前端尚未接入真实登录页面与 token 存储，目前仍依赖开发兜底模式访问后端。
- 当前环境中 `git` 命令不可用，暂时无法执行常规 git 状态检查。
- 当前轮次已完成代码级与构建级验证，但还缺少浏览器侧的自动截图验收。

## 明日待办

- 引入 `memory_chunks` 与 pgvector，推进真实检索链路。
- 接入真实 embedding 生成与 PostgreSQL / pgvector 检索。
- 将 QA 从当前启发式 chunk 召回提升到真实召回 + LLM 生成。
- 设计并接入前端登录流程，逐步收紧开发兜底访问模式。
- 将本地 `DATABASE_URL` 切换到 PostgreSQL 环境并验证迁移路径。
- 继续打磨前端细节，包括交互反馈、视觉验收与移动端适配。
