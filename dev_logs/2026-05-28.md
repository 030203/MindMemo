# 2026-05-28 开发日志

## 今日完成

- 按 Phase 9/10 到 Phase 11 前置方向，推进 QA 追问上下文与评测护栏小切片。
- 后端 `QARequest` 新增 `conversation_context`，旧客户端仍可只传 `question` 和 `mode`。
- QA 服务新增轻量追问识别与 effective question 构造，用最近对话上下文补全“那具体有哪些指标？”这类短追问。
- 检索 trace 元数据新增：
  - `effective_question`
  - `conversation_context_used`
- 前端 Chat 页面在提问时附带最近 4 条对话消息，支持连续追问。
- grounded QA fallback 在无外部 LLM 时会把命中片段写入回答，避免只返回标题。
- `eval_qa_grounding.py` 增加答案质量评测：
  - 必须包含关键术语
  - 禁止出现无关术语
  - 追问上下文是否被使用
- Golden dataset 升级到 `phase9_v3`：
  - 新增 relation-noise seed memories
  - 新增 relation-noise cases
  - 新增 follow-up question / answer-quality cases
- relation builder 增加负向语义过滤：当记录明确出现“无关 / 不是 / unrelated”等标记时，不再仅凭 Agent、Graph 等弱共享信号建立关系边。
- README 同步 QA 追问上下文与评测现状。
- 继续推进下一阶段小切片：Chat trace UI 展示追问改写后的 effective question。
- QA trace 新增 `workflow_steps`，用于描述路由、追问改写、chunk 检索、关系扩展、引用重排和回答生成步骤。
- Chat trace UI 新增“Agent 工作流”步骤列表，为后续 Phase 11 的 Agentic RAG workflow trace 做前置铺垫。
- README 同步 Agent workflow trace 当前状态与下一步。
- 继续推进 Phase 11：将轻量 workflow steps 升级为持久化 Agent run / step 模型。
  - 新增 `agent_runs`
  - 新增 `agent_run_steps`
  - QA trace metadata 回写 `agent_run_id` 与 `agent_run_status`
  - 新增 `GET /api/v1/qa/agent-runs`
  - 前端 API 类型补齐 `AgentRun` / `AgentRunStep`
  - 新增 `scripts/smoke_test_phase11_agent_runs.py`
- 继续推进 Phase 12：补齐独立 Insight API 与来源回链小切片。
  - 新增 `GET /api/v1/insights/overview`
  - 新增 `GET /api/v1/insights/expenses`
  - 新增 `GET /api/v1/insights/reflection`
  - 洞察卡片 schema 新增 `sources`
  - 首页洞察卡改为消费 `insights/overview`，并展示来源锚点
  - QA 的 `insight_agent` 回答改为优先返回 memory / todo 来源，而不是仅返回抽象 insight 标签
  - 新增 `scripts/smoke_test_phase12_insights_api.py`
- 继续推进 Phase 12 页面化小切片：新增独立个人洞察页。
  - 新增 `frontend/src/pages/InsightsPage.tsx`
  - 新增 `/insights` 路由与侧边栏入口
  - 页面支持总览 / 消费 / 复盘三类切换，分别承接 `/insights/overview`、`/insights/expenses`、`/insights/reflection`
  - 每张洞察卡展示来源回链，可跳回记忆详情或待办中心
  - 首页洞察区新增“查看洞察”入口
- 继续推进 Phase 12 洞察页钻取小切片。
  - 洞察页新增单张卡片聚焦筛选，支持通过 `card` URL 参数保留状态
  - 洞察页新增来源类型筛选，支持 `all` / `memory` / `todo`
  - 来源筛选会同步影响来源数量统计与每张卡片的来源列表
  - 切换总览 / 消费 / 复盘时自动清理已选卡片，避免保留无效筛选
- 继续推进 Phase 12 后端时间窗口筛选小切片。
  - `/api/v1/insights/overview`、`/expenses`、`/reflection` 新增 `window` 查询参数
  - 支持 `default`、`7d`、`30d`、`90d`、`month`、`all`
  - 默认口径保持兼容：消费仍按本月，状态/学习/项目仍按近 30 天
  - 前端洞察页新增时间窗口控件，使用 `window` URL 参数保留状态
  - 切换时间窗口时自动清理单张卡片筛选，避免跨窗口保留无效聚焦
- 继续推进 Phase 12 单张 Insight 详情 API 小切片。
  - 新增 `GET /api/v1/insights/{kind}`
  - 支持 `expense`、`mood`、`learning`、`plan`、`project`
  - 详情接口同样支持 `window` 查询参数
  - 前端洞察页在存在 `card` URL 参数时，会额外请求单张详情接口
  - 单张详情加载失败时保留列表结果作为兜底展示

## 验证记录

- 前端执行 `npm.cmd run build` 成功。
- 后端执行 `D:\py\anaconda\python.exe -m py_compile ...` 成功。
- `D:\py\anaconda\python.exe scripts\eval_qa_grounding.py` 通过：
  - `answer_nonempty_rate=1.0`
  - `citation_hit_rate=1.0`
  - `must_include_hit_rate=1.0`
  - `must_not_include_hit_rate=1.0`
  - `conversation_context_hit_rate=1.0`
- `D:\py\anaconda\python.exe scripts\eval_relation_expansion.py` 通过：
  - `relation_context_hit_rate=1.0`
  - `relation_expansion_hit_rate=1.0`
  - `relation_noise_free_rate=0.8571`
  - `relation_expansion_noise_free_rate=1.0`
- `D:\py\anaconda\python.exe scripts\eval_retrieval.py` 通过：
  - `average_recall_at_5=1.0`
  - `mean_reciprocal_rank=0.9375`
  - `fact_citation_hit_rate=1.0`
- 下一阶段 UI / trace 切片完成后再次验证：
  - `npm.cmd run build` 成功
  - `D:\py\anaconda\python.exe -m py_compile app\schemas\qa.py app\services\qa_service.py` 成功
  - `D:\py\anaconda\python.exe scripts\eval_qa_grounding.py` 通过
  - `D:\py\anaconda\python.exe scripts\eval_relation_expansion.py` 通过
  - `D:\py\anaconda\python.exe scripts\eval_retrieval.py` 通过
- Phase 11 持久化 Agent run / step 切片验证：
  - `D:\py\anaconda\python.exe -m py_compile app\models\evaluation.py app\repos\agent_run_repo.py app\services\agent_workflow_service.py app\services\retrieval_trace_service.py app\api\v1\qa.py scripts\smoke_test_phase11_agent_runs.py` 成功
  - `npm.cmd run build` 成功
  - `D:\py\anaconda\python.exe scripts\smoke_test_phase11_agent_runs.py` 通过，生成 trace 与 agent run，并持久化 4 个 step
  - `D:\py\anaconda\python.exe scripts\eval_qa_grounding.py` 通过
  - `D:\py\anaconda\python.exe scripts\eval_relation_expansion.py` 通过
  - `D:\py\anaconda\python.exe scripts\eval_retrieval.py` 通过
- Phase 12 Insight API / source-link 切片验证：
  - `D:\py\anaconda\python.exe -m py_compile app\schemas\dashboard.py app\services\dashboard_service.py app\services\qa_service.py app\api\v1\insights.py scripts\smoke_test_phase12_insights_api.py` 成功
  - `npm.cmd run build` 成功
  - `D:\py\anaconda\python.exe scripts\smoke_test_phase12_insights_api.py` 通过
  - `D:\py\anaconda\python.exe scripts\smoke_test_phase12_insight_agent.py` 通过
  - `D:\py\anaconda\python.exe scripts\smoke_test_phase12_expense_anomaly.py` 通过
  - `D:\py\anaconda\python.exe scripts\smoke_test_phase12_project_insight.py` 通过
- Phase 12 Insight 页面化切片验证：
  - `npm.cmd run build` 成功
  - 首次在沙箱中执行构建时 Vite 加载配置触发 `spawn EPERM`，授权后重跑通过
- Phase 12 洞察页钻取切片验证：
  - `npm.cmd run build` 成功
  - `http://127.0.0.1:5173/insights` 返回 `200`
  - 当前本地 Node 环境缺少 Playwright，未完成截图级浏览器验证
- Phase 12 后端时间窗口筛选切片验证：
  - `D:\py\anaconda\python.exe -m py_compile app\services\dashboard_service.py app\api\v1\insights.py scripts\smoke_test_phase12_insights_api.py` 成功
  - `D:\py\anaconda\python.exe scripts\smoke_test_phase12_insights_api.py` 通过
  - `npm.cmd run build` 成功
  - smoke 已覆盖 `window=7d` 正常返回，以及非法 `window=invalid` 返回 `422`
- Phase 12 单张 Insight 详情 API 切片验证：
  - `D:\py\anaconda\python.exe -m py_compile app\services\dashboard_service.py app\api\v1\insights.py scripts\smoke_test_phase12_insights_api.py` 成功
  - `D:\py\anaconda\python.exe scripts\smoke_test_phase12_insights_api.py` 通过
  - `npm.cmd run build` 成功
  - smoke 已覆盖 `/api/v1/insights/expense?window=7d` 正常返回，以及非法 kind 返回 `422`
- Phase 12 Insight cache 与截图级 UI 验收切片：
  - 洞察卡片增加按 user/window 维度的短 TTL 内存缓存。
  - 记忆创建/更新、待办创建/更新/完成、Review 转待办后会失效对应用户洞察缓存。
  - `scripts/smoke_test_phase12_insights_api.py` 增加重复请求缓存命中验证。
  - 使用浏览器打开 `/insights` 完成截图级验收，无前端 console error，截图保存到 `dev_logs/phase12-insights-cache-preview.png`。
- 产品 UI 收拢重构切片：
  - 将前端从深色多模块工作台改为浅色一页式个人记录界面。
  - 顶层导航删除“收集 / 时间线 / 待确认 / 洞察 / 待办”等显性入口，旧路径统一回到首页，保留记忆详情隐藏路由用于来源回链。
  - 首页重构为三块：超级输入区、记录流与待办卡片、可唤起 AI 对话抽屉。
  - 超级输入区支持 Enter 直接保存、截图/文件选择、拖拽文件、粘贴图片；PDF 复用已有导入接口，其余附件先保存为记录上下文。
  - 记录流混合展示最近记忆和未完成待办，每张卡片提供轻量提醒设置与“和 AI 聊聊”入口。
  - AI 抽屉可全局唤起，也可带着单条记录上下文发起对话。
- 产品 UI 双栏 Copilot 重构切片：
  - 将首页从单列居中改为 `记录流 / AI Copilot` 左右分栏。
  - 左侧约 40%-50% 宽度，保留万能输入框和最近记录流，并支持独立滚动。
  - 右侧约 50%-60% 宽度，AI 对话常驻展开，不再依赖 FAB 或抽屉唤起。
  - 点击左侧卡片的对话按钮后，该记录会作为上下文胶囊进入右侧 Copilot，并自动聚焦 AI 输入框。
  - 移动端降级为上下布局，先记录流、后 Copilot。
- 根据界面反馈撤回双栏 Copilot 切片。
  - 首页恢复到上一版单列居中布局。
  - AI 恢复为右下角 FAB + 可唤起抽屉。
  - 删除双栏布局 CSS 覆盖，保留浅色一页式主页和记录流设计。
- 记录详情页 UI 优化切片。
  - 将详情页从纯白卡片堆叠改为带浅蓝信息横幅的详情工作台。
  - 顶部集中展示分类、来源、状态、标题、摘要、重要性和记录时间。
  - 原文记录与 AI 理解结果改为更明确的左右分栏，并给原文区域增加浅灰底阅读容器。
  - 相关记忆卡片、事实卡片、标签区域增加边框、轻阴影和浅色强调，降低“全白”感。
- 设置入口与个人偏好切片。
  - 顶部导航恢复独立设置按钮，受保护路由接回 `/settings`。
  - 设置页重构为个人信息、后台通知、AI 配置摘要三块，保持浅色极简风格。
  - 后端新增 `PATCH /api/v1/auth/me`，支持更新显示名称与邮箱，并同步前端会话缓存。
  - 验证：`D:\py\anaconda\python.exe -m py_compile app\schemas\auth.py app\services\auth_service.py app\api\v1\auth.py` 成功。
  - 验证：`npm.cmd run build` 成功。
  - 验证：浏览器打开 `http://127.0.0.1:5173/settings`，设置页可访问且无前端 console error。

## 风险 / 阻塞

- 当前追问补全仍是轻量规则，不是完整 query planner；后续进入 Phase 11 时需要显式记录规划步骤和工具选择。
- 本地 PowerShell 沙箱中直接运行 `python.exe` 会被拒绝访问，本次后端验证使用授权后的 `D:\py\anaconda\python.exe`。
- Phase 12 已补独立 Insight 页面、轻量钻取筛选、后端时间窗口筛选、单张 Insight 详情 API、Insight cache 和截图级 UI 验收；后续可进入 Phase 13 长期 Reflection 切片。
- 新 UI 已完成代码层收拢，但截图/真实交互验收还需再跑一次，尤其是文件拖拽、移动端底部输入体验和 AI 抽屉遮挡关系。
- 双栏 Copilot 已撤回；后续如需强化 AI 权重，应重新评估更轻量的布局方案，例如扩大抽屉默认宽度、增加首页 AI 预览区，或提供可切换的专注对话模式。
- 详情页已做视觉层次优化，但仍未做截图级验收；后续可继续微调不同记录类型的顶部强调色。

## 明日待办

- 为 Agent run / step 增加真实耗时、错误状态和失败原因记录。
- 继续推进 Agentic RAG：增加显式子问题拆解和工具选择计划。
- 评估进入 Phase 13：Daily / Weekly Reflection 与长期记忆压缩小切片。
- 对一页式浅色 UI 做浏览器截图验收，并补齐真实附件存储/OCR 能力。
- 评估 refresh-token revocation 与设备会话管理的小切片。
